= turbo_stream.replace 'remote_modal' do
  = render partial: 'categories/form', layout: 'layouts/main_modal',
                                     locals: { title: "Update Category",
                                               submit_text: 'Update' }
